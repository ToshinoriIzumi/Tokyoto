<h1>プロフィール詳細</h1>
<div>メールアドレス <%= current_user.email %></div>
<div>ユーザーネーム <%= current_user.user_name %>さん</div>
<div>住んでいる地域 <%= show_city_name %></div>
<div>収入（単位：万円）<%= show_income %>万円</div>
<div>子どもの生年月日
  <% current_user.children.each_with_index do |child, index|%>
    <%= "第" + (index + 1).to_s + "子:" %>
    <%= child.age %>歳
  <% end %>
</div>
<div>ステータス
  <% current_user.family_situations.each do |family_situation|%>
    <%= family_situation.situation %>
  <% end %>
</div>


申し込みができる制度は<%= @supports.count %>個です
<% @supports.each do |support| %>
  <div>
    <%= support.support_name %>
  </div>
<% end %>
<%= link_to  "プロフィールを編集する", edit_profile_path %>
<%= link_to "トップページへ", top_index_path %>
<%= link_to "退会する", user_path(current_user.id), method: :delete, "data-confirm" => "本当に退会しますか？" %>
